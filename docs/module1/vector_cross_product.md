Cross Product

> **[figure]**
> We may use Cookies OK
> long it is) and direction:

*[figure]*

A [vector] has magnitude (how long it is) and direction:

*[figure]*

Two vectors can be multiplied using the "Cross Product" (a/so see (Bot Product )

*[figure]*

The Cross Product a X b of two vectors is another vector that is at right angles to both:

And it all happens in 3 dimensions!

The magnitude (length) of the cross product equals the | area of a parallelogram | with vectors a and b for sides:

https:/Awww.mathsisfun.com/algebra/vectors-cross-product.html

Cross Product

*[figure]*

*[figure]*

See how it changes for different angles:

*[figure]*

The cross product (blue) is:

zero in length when vectors a and b point in the same, or opposite, direction reaches maximum length when vectors a and bD are at right angles

And it can point one way or the other!

So how do we calculate it?

### Calculating

WE CAN CALCULATE THE CROSS PRODUCT THIS WAY:

https:/Awww.mathsisfun.com/algebra/vectors-cross-product.html

Cross Product

## a xb = fa] |b] sin(6) n

- e |al| is the magnitude (length) of vector a e |b| is the magnitude (length) of vector b **e** @ni i **s** the theau**n** gleit vector betwe aten a andright angles b to both a and b

*[figure]*

So the length is: the length of a times the length of b times the sine of the angle between a and b,

Then we multiply by the vector n so it heads in the correct direction (at right angles to both a and b).

##### OR WE CAN CALCULATE IT THIS WAY:

When a and b start at the origin point (0,0,0), the Cross Product will end at: @ c, = ayb, — azby _ @ cy = a,b,—ayb,

> **[figure]**
- > (by, by
> ID Xx? wy? Zz
> 1"

> **[figure]**
> XPV De
- > axb (by, by
> ID Xx? wy?
> 1"
> a
> (ax, ay, az)

Example: The cross product of a = (2,3,4) and b = (5,6,7)

@ cy, = ayb,—azby = 3x7—4x6 =—3 @ cy, = azby—ayby = 4x5—2x7 = 6 @ c, = aby—ayby = 2x6—3x5 =—-3

Answer: a X b =(-3,6,-3)

https:/Awww.mathsisfun.com/algebra/vectors-cross-product.html

Cross Product

# axb A

### Which Direction?

*[figure]*

The cross product could point in the completely opposite direction and still be at right angles to the two other vectors, so we have the:

"Right Hand Rule"

With your right-hand, point your index finger along vector a, and point your middle finger along vector b: the cross product goes in the direction of your thumb.

#### Dot Product

The Cross Product gives a vector answer, and is sometimes called the vector product.

But there is also the which gives a scalar (ordinary number) answer, and is sometimes called the scalar product.

2d Question: What do you get when you cross an elephant with do you get when you cross an elephant with you get when you cross an elephant with get when you cross an elephant with when you cross an elephant with you cross an elephant with cross an elephant with an elephant with elephant with with a banana? banana? Answer: |elephant| |banana| sin(8) n

> **[figure]**
> 2d Question: What do you get when you cross an elephant with do you get when you cross an elephant with you get when you cross an elephant with get when you cross an elephant with when you cross an elephant with you cross an elephant with cross an elephant with an elephant with elephant with with a banana? banana?
> Answer: |elephant| |banana| sin(8) n

Mathopolis: Qi Q2 Q3 Q4 Q5 Q6 Q7 Q8 QI Q10

https:/Awww.mathsisfun.com/algebra/vectors-cross-product.html

---

## Reference — canonical source text

*Retrieved 2026-08-27 from [Math is Fun — Cross Product](https://www.mathsisfun.com/algebra/vectors-cross-product.html), the source this handout was printed from. The scan above lost most of the formula subscripts; this section restores them.*

### Definition

The cross product **a × b** of two vectors is **another vector** that is at right angles to both. It is defined in three dimensions.

### Magnitude form

```
a × b = |a| |b| sin(θ) n
```

where `|a|` and `|b|` are the magnitudes of **a** and **b**, `θ` is the angle between them, and `n` is the unit vector at right angles to both.

The magnitude `|a × b|` equals the area of the parallelogram spanned by **a** and **b**.

### Component form

For vectors starting at the origin (0, 0, 0):

```
cx = ay·bz − az·by
cy = az·bx − ax·bz
cz = ax·by − ay·bx
```

### Worked example

For **a** = (2, 3, 4) and **b** = (5, 6, 7):

```
cx = 3×7 − 4×6 = −3
cy = 4×5 − 2×7 =  6
cz = 2×6 − 3×5 = −3

a × b = (−3, 6, −3)
```

### Properties

- The cross product is **zero** when the vectors are parallel or antiparallel (sin 0° = 0).
- It is **maximal** when the vectors are perpendicular (sin 90° = 1).
- **Right Hand Rule** for direction: index finger along **a**, middle finger along **b**, thumb points along **a × b**.
- Contrast with the [dot product](vector_dot_product.md), which yields a scalar rather than a vector.
